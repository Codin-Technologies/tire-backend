services:
  - type: web
    name: tire-backend
    env: docker
    dockerfilePath: ./Dockerfile.prod
    plan: free
    region: frankfurt
    envVars:
      - key: APP_KEY
        generateValue: true
      - key: APP_ENV
        value: production
      - key: APP_DEBUG
        value: true
      - key: DB_CONNECTION
        value: pgsql
      - key: DATABASE_URL
        fromDatabase:
          name: tire-db
          property: connectionString
      - key: DB_URL
        fromDatabase:
          name: tire-db
          property: connectionString
      - key: LOG_CHANNEL
        value: stderr
      - key: APP_URL
        value: https://tire-backend-h8tz.onrender.com
      - key: L5_SWAGGER_CONST_HOST
        value: https://tire-backend-h8tz.onrender.com
databases:
  - name: tire-db
    plan: free
    region: frankfurt
